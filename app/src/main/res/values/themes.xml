<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mqtt1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Usamos los nuevos colores modernos -->
        <item name="colorPrimary">@color/neon_blue</item>
        <item name="colorPrimaryVariant">@color/bg_app_start</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/bg_app_start</item>
    </style>

    <style name="Theme.Mqtt1" parent="Base.Theme.Mqtt1" />
    
    <!-- Estilo Imagen Circular -->
    <style name="CircleImageStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- Estilo para el BottomSheet (Menú deslizante redondo) -->
    <style name="BottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>
    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_glass_effect</item>
    </style>
</resources>